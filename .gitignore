# Dependencies
node_modules/

# Build output
dist/

# Artifacts (project-specific index output)
artifact/

# OS files
.DS_Store

# Editor
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local
.playwright-mcp
.footnote/

# Third-party eval corpora (fetched, not vendored)
data/*.txt

# E3C corpus (CC BY-NC, fetched not vendored)
data/e3c_en/

# OCR spike artifacts: rendered page images + per-page duplicates (regenerate with
# ocr-colab.py). The 3 ocr-gundam-results/*/FULL.md ARE tracked — bench-chunking.ts
# reads them as the unlimited-ocr benchmark row's input.
ocr-colab-results/
ocr-space/
ocr-gundam-results/*/page_*/
